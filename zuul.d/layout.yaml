- project:
    check:
      jobs:
        - tripleo-ci-centos-8-scenario001-standalone:
            nodeset:
              nodes:
                - name: primary
                  label: rdo-centos-stream
            vars:
              build_container_images: true
