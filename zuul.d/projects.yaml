- project:
    name: rdo-jobs
    check:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - periodic-tripleo-ci-centos-7-standalone-master:
            files:
              - zuul.d/standalone-jobs.yaml
        # for testing this patch only
        - staging-tripleo-ci-base-singlenode-rdo
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
