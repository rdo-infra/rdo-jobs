- project:
    name: rdo-jobs
    check:
      jobs:
        - periodic-tripleo-centos-7-rocky-containers-build-push
        - tox-docs:
            nodeset: container-centos
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
