- project:
    name: rdo-jobs
    templates:
    check:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - tripleo-build-containers-fedora-28:
            files:
              - ^zuul.d/build-containers.yaml$
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
