- project:
    name: rdo-jobs
    check:
      jobs:
        - periodic-tripleo-rhel-8-master-containers-build-push:
            vars:
              force_periodic: true
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - tox-linters
