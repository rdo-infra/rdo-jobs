- project:
    name: rdo-jobs
    check:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - periodic-tripleo-ci-centos-7-standalone-master:
            files:
              - zuul.d/standalone-jobs.yaml
        - registry-login-integration:
            files:
              - ^roles/registry-login/.*$
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
