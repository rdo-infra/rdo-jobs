- project:
    name: rdo-jobs
    check:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - tox-linters
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - tox-linters
