- project:
    name: rdo-jobs
    check:
      jobs:
        - periodic-tripleo-centos-7-master-containers-build-push
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - tox-linters
