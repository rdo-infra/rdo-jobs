- project:
    name: rdo-jobs
    templates:
      - tripleo-build-containers-jobs:
          files:
            - ^zuul.d/build-containers.yaml$
    check:
      jobs:
        - tox-docs:
            nodeset: container-centos
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
