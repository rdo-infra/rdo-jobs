- project:
    name: rdo-jobs
    check:
      jobs:
        - tox-docs:
            nodeset: container-centos
        - periodic-tripleo-ci-centos-7-standalone-upgrade-master
    gate:
      jobs:
        - tox-docs:
            nodeset: container-centos
