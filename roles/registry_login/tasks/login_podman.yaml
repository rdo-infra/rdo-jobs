- block:
    - name: Login to registry via podman
      command: "podman login {{ registry_host }} --username {{ registry_username }} --password '{{ registry_password }}' --tls-verify false"
      no_log: true
  rescue:
    - debug:
        msg: "Warning: Unable to Login with podman"
  become: true
