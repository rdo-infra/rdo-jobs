- block:
    - name: Login to registry via docker
      command: "docker login {{ registry_host }} --username {{ registry_username }} --password '{{ registry_password }}'"
      no_log: true
  rescue:
    - debug:
        msg: "Warning: Unable to Login with docker"
  become: true
