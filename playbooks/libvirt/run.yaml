---
- hosts: all
  tasks:

    - name: Run tox
      become: true
      command:
        cmd: tox
        chdir: c"{{ ansible_user_dir }}/{{ zuul.projects['github.com/pycontribs/molecule-libvirt'].src_dir }}"
      args:
        warn: false
      changed_when: true
