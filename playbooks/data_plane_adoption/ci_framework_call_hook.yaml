- hosts: all
  vars:
    framework_dir: "/home/zuul/src/github.com/openstack-k8s-operators/ci-framework"
  tasks:

    - name: Call run_hook role
      vars:
        step: pre_tests
      ansible.builtin.import_role:
        name: run_hook
